[package]
name = "pglite-examples"
publish = false
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
pglite = { path = "../crates/pglite" }
futures = { version = "0.3", features = ["executor"] }
